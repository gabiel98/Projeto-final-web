<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  <title>Inventário da Loja</title>
  <link rel="stylesheet" href="/css/inventory.css">
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <h1>Inventário da Loja</h1>
    <p>Aqui você pode cadastrar, editar e excluir itens.</p>
    <p><a href="/products/new">➕ Cadastrar novo produto</a></p>
    <% if (!produtos || produtos.length === 0) { %>
      <p>Nenhum produto cadastrado.</p>
    <% } else { %>
      <ul>
        <% produtos.forEach(function(p) { %>
          <li>
            <% if (p.imagem) { %>
              <img src="<%= p.imagem %>" alt="<%= p.nome %>" style="max-width:80px;height:auto;vertical-align:middle;margin-right:10px" />
            <% } %>
            <strong><%= p.nome %></strong> — R$ <%= typeof p.preco === 'number' ? p.preco.toFixed(2) : p.preco %>
            <br/>
            <small>Tipo: <%= p.tipo || 'Outro' %> | Estoque: <%= p.estoque || 0 %> | <%= p.descricao %></small>
            <div style="margin-top:6px">
              <a href="/products/<%= p._id %>/edit"><button>✏️ Editar</button></a>
              <form action="/products/<%= p._id %>/delete" method="POST" style="display:inline">
                <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <% } %>
                <button type="submit" style="color:red">🗑️ Excluir</button>
              </form>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </main>
  <%- include('partials/footer') %>
</body>
</html>
