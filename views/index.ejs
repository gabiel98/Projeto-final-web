<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  <title>Produtos</title>
  <!-- Estilos podem ser movidos para um arquivo CSS separado -->
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <h1>PokeShop — Itens Pokémon</h1>
    <p>Itens e colecionáveis do universo Pokémon.</p>
    <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
      <p>Conectado como: <strong><%= userName %></strong><% if (typeof userCargo !== 'undefined' && userCargo) { %> (<%= userCargo %>)<% } %></p>
    <% } else { %>
      <p><a href="/login">Faça login</a> ou <a href="/users/new">crie sua conta</a> para aproveitar mais recursos.</p>
    <% } %>
    <p><a href="/cart">Ir para o carrinho</a></p>
    <div class="grid">
      <% produtos.forEach(function(p) { %>
        <div class="card">
          <h3><%= p.nome %></h3>
          <div class="price"><%= p.preco %></div>
          <p><%= p.descricao %></p>
          <div style="margin-top:8px">
            <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
              <form action="/cart/add" method="POST" style="display:inline">
                <input type="hidden" name="productId" value="<%= p._id %>" />
                <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <% } %>
                <button type="submit">Adicionar ao carrinho</button>
              </form>
            <% } %>
            <% if (typeof userRole !== 'undefined' && (userRole === 'dono' || userRole === 'funcionario')) { %>
              <a href="/products/<%= p._id %>/edit"><button>✏️ Editar</button></a>
              <form action="/products/<%= p._id %>/delete" method="POST" style="display:inline">
                <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <% } %>
                <button type="submit" style="color:red">🗑️ Excluir</button>
              </form>
            <% } %>
          </div>
        </div>
      <% }) %>
    </div>
    <% if (typeof userRole !== 'undefined' && (userRole === 'dono' || userRole === 'funcionario')) { %>
      <p><a href="/products/new">➕ Adicionar novo produto</a></p>
    <% } %>
  </main>
</body>
</html>
